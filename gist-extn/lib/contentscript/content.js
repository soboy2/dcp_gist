chrome.extension.onMessage.addListener(function(message, sender, sendResponse) {
    // switch(message.type) {
    //
    //
    //     break;
    // }
});

var content = {

    init: function(){
        var kg = this;

        util.init();
    }

};

content.init();
